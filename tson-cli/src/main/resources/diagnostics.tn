!!id:"https://tson.io/2026/32/ltr8/cli/diagnostics-1.tn"
!!meta:"https://tson.io/2026/32/m/meta.tn"
!!import:"https://tson.io/2026/32/m/core.tn"
@doc:"""
  This project's own schema !!id convention (see CLAUDE.md's "Project-owned schema !!id
  convention"): /2026/32 is the spec revision this schema is written against, ltr8 is the
  publishing org, cli is this module's own schema group, and the trailing -1 is this schema's
  own version -- bump it, under a new name, whenever this shape changes; never edit a published
  schema's own content in place.
  """
{
  diagnostic => { code: text  message: text }
  validation_report => { valid: boolean  errors: [diagnostic] }
}
